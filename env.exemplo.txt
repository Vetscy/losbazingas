# Copie este arquivo para `.env` na raiz do projeto e preencha.

# Credenciais da aplicação (Discord Developer Portal > sua app > OAuth2)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Origem PÚBLICA do servidor — a URL do túnel ou do seu domínio.
# IMPORTANTE: NÃO use o dominio *.discordsays.com aqui.
# A página de captura precisa abrir como página normal (top-level),
# fora do proxy do Discord, senão getDisplayMedia é bloqueado.
# Ex: https://algo-aleatorio.trycloudflare.com
PUBLIC_ORIGIN=http://localhost:3001

# Segredo para assinar os tokens de sala (qualquer string longa e aleatória)
SESSION_SECRET=troque-isto-por-algo-aleatorio-e-longo

PORT=3001

# Em dev permite entrar na sala sem passar pelo Discord (rota /api/session-dev).
# Deixe como "production" ao publicar.
NODE_ENV=development
